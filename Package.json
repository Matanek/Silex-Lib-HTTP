{
  "name": "HTTP",
  "version": "0.6.0",
  "description": "Bounded HTTP/1.1 types, reusable clients, and application servers.",
  "authors": ["Matanek"],
  "requires": {
    "silex": ">=0.39.0"
  },
  "dependencies": {
    "STD": "^0.17.0"
  },
  "devDependencies": {
    "HTML": "^0.3.0",
    "JSON": "^0.1.1"
  }
}
